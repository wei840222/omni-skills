# Setup — Pandas

Read this when `<state_root>/` doesn't exist. Create the folder and initialize `memory.md`.

## Data Storage

All preferences are stored locally in `<state_root>/memory.md`. Users can view, edit, or delete this file anytime.

## First Conversation

Ask what would help them most:
- Level of intervention (proactive tips vs on-demand)
- Typical data work (time series, transactional, logs)

Save answers to `<state_root>/memory.md` using the template from `references/memory-template.md`.

## Ongoing Use

- Check `<state_root>/memory.md` at start of each session
- Update preferences when user shares new information
- Provide pandas guidance based on stored context
