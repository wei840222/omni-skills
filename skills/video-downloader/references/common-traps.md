# Common Traps

- Downloading playlists instead of single videos -> use `--no-playlist` by default.
- Choosing `best` for limited storage -> oversized files and slow transfers.
- Re-trying blocked URLs excessively -> temporary ban risk and no progress.
- Saving with title only -> filename collisions across similar uploads.
- Executing downloads before validating metadata -> wrong media downloaded from redirected links.
