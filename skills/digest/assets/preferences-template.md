# Digest Preferences

Auto-evolving user preferences. Edit as you learn. Dimension catalog and defaults: `dimensions.md`.

**Signal counting:**
- An explicit statement ("shorter", "drop sports") counts as 2 signals; one behavioral observation (skipped, forwarded, asked a follow-up) counts as 1.
- 2+ signals → mark `pattern`. Ask once before acting on format/timing patterns; exclusions are the exception — apply at pattern strength immediately (SKILL.md rule 3), still confirm before locking.
- User explicitly approves when asked → `confirmed`.
- Confirmed + 3 deliveries without contradiction → `locked`.
- Demotion: any contradicting signal drops the entry one level; an explicit reversal ("actually, include sports again") replaces the entry the same day, at `confirmed`.

**Worked example:** user skips sports items in two digests → `exclude: sports (pattern)`, sports dropped now → ask once: "drop sports going forward?" → "yes" → `(confirmed)` → three digests later, no pushback → `(locked)`.

---

### Content
<!-- Topics they want. Format: "topic: interest-level (level)" -->
<!-- Topics to exclude. Format: "exclude: topic (level)" -->

### Sources
<!-- Trusted sources. Format: "source: trust-level (level)" -->
<!-- Blocked sources. Format: "block: source (level)" -->

### Format
<!-- Delivery format preferences. Format: "dimension: preference (level)" -->

### Timing
<!-- When they want digests. Format: "schedule: timing (level)" -->

### Prioritization
<!-- What to highlight vs bury. Format: "priority: rule (level)" -->
<!-- Urgent-signal rules. Format: "urgent: condition (level)" — interrupts require confirmed+ -->

### Depth
<!-- How detailed per topic. Format: "topic: depth (level)" -->

---

## Context Profiles

For users with multiple digest contexts (e.g., work morning vs personal evening):

### Profile: [name]
<!-- Overrides for this context; anything unset falls through to the sections above -->

---

*Empty sections = defaults apply. Observe and evolve.*
