# Quality Score

- Use the Quality Score diagnostic to investigate expected click-through rate, ad relevance, and landing-page experience for eligible search keywords.
- Review the diagnostic component that is weak before changing ads or landing pages; use the same intent across the keyword, ad, and landing page.
- Treat Quality Score as a diagnostic signal, not a promised CPC discount or a standalone optimization target.
